# FAEVault 4.0.0

发布日期：2026-08-14

4.0.0 是 FAEVault 的大版本升级，Android 与 Windows 桌面端同步发布。本次升级重构了底层数据库格式（PMVE）、重写了局域网与云端同步链路、强化加密与设备身份安全，并将大文件与媒体存储改为流式高性能处理。

## 新增

- 全新数据库引擎 PMVE（Android / PC）：增量提交、块级加密、对象索引、媒体与附件对象化存储、自动压缩，支持大数据量与超大文件，并带完整性校验。
- 局域网通信升级：Spake2 双向认证、二维码 / PIN / 证书指纹校验、传输站按连接类型自动识别（导入确认 / 同步 / 文件传输）；Android / PC 行为、文案与错误处理全面对齐。
- 云同步双目标独立：云端硬盘与 WebDAV 独立关联、独立自动同步周期与失败计数，互不干扰。
- 本地备份（Android / PC）：本机目录或外接 U 盘 / 硬盘，按周期（实时至每月）自动覆盖写入；基于设备唯一身份识别目标盘，避免盘符变化导致误覆盖。
- 恢复密钥与密码重置流程重构（Android）：恢复密钥重新发放与保存、验证后立即解锁并进入改密流程。
- 中英文界面全量覆盖：硬编码文案迁移至标准资源，切换语言即时生效。

## 改进

- 自动填充（Android）：索引化匹配、应用名称兜底显示、无匹配时提供密码生成并填充、Inline Autofill 兼容回退。
- Markdown 模块（Android / PC）：多行文本升级为 Markdown，全格式渲染、编辑 / 查看切换、详情页格式化查看与一键复制。
- 高清图像查看：屏幕预览 + 可视区域解码、双击两级放大、多图滑动切换，超大图内存可控。
- 大文件与媒体：同步与内容传输流式化（2GB/10GB），内存占用恒定；媒体文件静态加密。
- 安全设置：截屏保护与后台界面隐藏拆分为独立开关；设备身份绑定与认证。

## 修复

- 云端自动同步双目标相互干扰的问题。
- 复制证件号等敏感内容提示未翻译、证件类型切换语言残留中文的问题。
- 局域网导入 / 同步 / 文件传输的连接校验、超时与结果弹窗问题。
- 导入大量图片 / 附件后详情页图像丢失、占位提示未居中等问题。

## 安装

完整步骤见 [安装与升级指南](./INSTALLATION.md)，其中包含 Windows、Android、Chrome、Edge 和 Firefox 浏览器扩展教程。
